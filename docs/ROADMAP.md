# 未完成清单

回答一个问题：**现在这个版本，还缺什么。**

分四档，按「用户实际会撞上的程度」排。已做的功能见 [`MVP.md`](MVP.md)。

---

## 一、会让人想摔鼠标的

| 缺口 | 现状 | 说明 |
|---|---|---|
| **非 UTF-8 文件会乱码** | `readFile(path, 'utf-8')` 写死 | 国内大量 TXT 是 GBK/GB18030，打开就是一片乱码。需要探测编码或让用户选 |
| **同一位置重复收藏会生成两条** | 阅读时的划词收藏没去重 | 手动词已经去了，划词这条还没有（手滑连点两次就多一条）|
| **手动词的去重只看原词** | 见上方「已修（第十六轮）」 | `Habits` 在一词已存在时还会被存下，然后合并成 ×2 |
| **没有自动备份** | 见下方「三」 | `inkpick-store.json` 损坏时退回空库，行为上对但没恢复手段 |

> 原来列在这里的「删了没法撤销 / 笔记不能编辑 / 文档不能删除重命名」已在第十三轮做掉，
> 见 [`MVP.md`](MVP.md) 的已做清单。

## 已修（第十四轮）

用户实际使用后反馈的三个问题：

- **侧栏开关位置奇怪、按钮太小** —— 已挪到内容区左上角（紧贴侧栏边界）并放大到 34px
- **搜索会把人拉走，且回不去** —— 改成输入时只高亮**不跳转**，回车/上下键才跳；
  另加「回到原处」。存的是**正文偏移量**而不是 scrollTop：搜索框一出现头部会变高，
  可滚范围跟着变，按像素记会回不准
- **设置按钮靠点循环切换** —— 收进「Aa」面板，四个原生下拉框直选
- **面板又要两步** —— 改成工具栏一行三段开关：每个选项一个按钮，一次点击到位
- **最右侧的原生滚动条是多余的** —— 上方已有可拖的进度条（还带百分比），
  右侧再挂一条重复且占 15px 正文字宽，已隐藏。
  同时给滚动容器加了 `tabIndex`，保证隐藏后键盘翻页仍然可用
- **侧栏的原生滚动条太突兀** —— 改成自绘的细条（跟着主题变色），
  并给文档区加了 38% 高度上限，文档多了不会把标注区挤没

## 已修（第十五轮）

**章节功能（V1）**。用户反馈「上游阅读软件都有章节」，本轮做了：

- `src/core/chapters.ts`：从纯文本里认出章节。实测（仿真中文 TXT）：
  **400 章 / 6.7MB 扫一遍 16ms**，1000 章 / 22.4MB 只要 48ms，**误判 0**
- 侧栏第一个区改成「文档 / 目录」两个标签：选书和选章是同一类事（导航），
  这样侧栏仍然只有两个滚动区，不会变成三段抢空间
- 头部显示当前章名 + `3 / 340`，两侧是上一章 / 下一章按钮
- 章尾一条「本章完」分隔线
- 快捷键 `[` `]` 翻章（在输入框里不抢键）
- 整本没有章节标记时（如大多数英文小说）**整个功能消失**，不给一堆垃圾章节

设计上的取舍：

- 章节**不进存档**。它是从正文纯函数推导出来的（才 16ms），因此不用改存储格式，
  而且识别规则以后改进了，旧书自动受益；存进去的话规则一改旧数据就永远过期
- 现在是「全书连续滚动 + 章节跳转」（方案 A）。**一次只显示一章**（方案 B）
  留待试用一段时间后再决定 —— B 要改搜索/跳转/恢复三处，而且会带来
  「每章都要点一下」的摩擦
- 「上一章」按阅读软件的习惯：**先回本章开头，已在开头才去上一章**

本轮还修了一个**长期存在的假失败**：侧栏拖宽的两条用 `expect(await …)` 裸读，
而 pointermove 是「连续事件」，React 会批处理，`page.evaluate` 可能在渲染前执行，
于是读到拖拽前的宽度 —— 实测约一半概率假失败。已改成 `expect.poll`。

## 已修（第十六轮）

**手动记词** —— 用户提的场景：刷视频 / 读论文时遇到生词，想顺手记下。
这不是「阅读器的附属功能」，而是把应用从「读书时才开」变成「每天都开」：
捕获（碎片时间）→ 复习（每天几分钟）→ 回到原文，是一个闭环。

本轮做的是**最简版**（窗口内加词 + 无书也能用），全局快捷键与复习调度留给后面。

- 数据模型：`Annotation` 的 `docId` 与 `anchor` 改成可选，
  **缺这两个就是「手动记的词」**
- 侧栏标注区右上角加「＋ 单词」；回车存下后**清空并保持焦点**，
  可以连着记一串而不用碰鼠标；欢迎页也有入口（侧栏收着时会先展开）
- 释义自动查（同一套 `lookupWord`），查不到就手写（复用已有的 `manualDefinition`）
- 同一个词重复记会被拦住（按 lemma **和**当时输入的原词都比一遍）
- 词表里标出「手动」来源

顺手修的两处结构性问题：

- **「全部文档」→「全部」**，且**不再在没有文档时被禁用**。
  在此之前，一个只想记单词、不导入书的用户，两个范围按钮都是灰的
  —— 根本看不到自己的词表
- **类型筛选的「全部」→「不限」**。范围那一行已经有「全部」了，
  同一个面板里两个「全部」分不清谁是谁

设计取舍：

- **不额外存 `source` 字段**，直接由「缺不缺 docId/anchor」派生。
  存两份事实早晚会出现「source 说是手动、却有 anchor」的脏数据；派生比存储可靠
- **校验写成「两者必须同进同退」**：都有 = 来自阅读，都没有 = 手动，
  只有一半 = 脏数据丢掉。而不是把半残数据带进内存里让它到处判空
- 手动词**不用逼用户填句子**：以后在书里划到同一个词会自动归到同一组，
  语境就补上了（按 lemma 分组是本来就有的行为，一行新代码都没写）

已知代价（已记入下方「一」档）：

- 去重只能拿**输入的原词**去比。`Habits`（词典还原成 `habit`）在一词已存在时
  仍然会被存下，然后在列表里合并成 ×2
- 旧版本打开新存档时，手动词会被旧的严格校验丢掉（“降级会丢字段”那个缺口的下一个具体例子）

## 二、功能上确实没有的

| 缺口 | 说明 |
|---|---|
| ~~搜索~~ | ✅ 已做（见 MVP.md 的已做清单） |
| ~~目录~~ | ✅ 已做（第十五轮，见上方「已修」）。还剩：卷/章两级缩进、进度条上的章节刻度 |
| **高亮颜色** | 现在词条是黄、笔记是蓝，固定配色，不能选 |
| **lemma 归错了没法手动改** | `left → leave` 这类误判只能忍 |
| **键盘快捷键** | 没有。翻页 / 搜索 / 收藏全靠鼠标 |
| **没有自动备份** | `inkpick-store.json` 损坏时 `deserializeStore` 会退回空库 —— 行为上是对的（不崩），但等于标注全丢，且没有恢复手段。另外每改一条标注都会重写整个文件（含全书正文），大书时值得观察 |

## 三、工程完备性

| 缺口 | 说明 |
|---|---|
| **打包** | 没接 electron-builder，现在只能 `npm run dev` / `npm start` |
| **应用名未定死** | 打包前必须定。Electron 的 userData 目录名优先取 `productName`，一旦和 dev 时不一致，会以为「数据全没了」（其实在旧目录） |
| **代码签名** | 没有证书，Windows 首次安装会弹 SmartScreen 警告 |
| **CSP** | 没配。正文全程用 React 文本节点渲染（无 `dangerouslySetInnerHTML`）且不加载远程内容，风险可控，但打包前应补上 |
| **Lint** | 完全没接。`AGENTS.md` 里写明「补上之前不要假装跑过」 |
| **降级会丢字段** | `deserializeStore` 只保留认识的字段。将来加了新字段后，用旧版本打开再保存，新字段会被静默丢掉 |
| **词库更新要重新打包** | `mini.tsv` 用 `?raw` 编进了主进程 bundle。想独立更新词库得改成 `extraResources` / userData 读取 |

## 四、明确不做（及替代方案）

| 不做 | 替代 |
|---|---|
| **EPUB** | 暂缓。真要做得动渲染引擎、CFI 定位、重定位重写三块，见 [`MVP.md`](MVP.md) |
| **复习算法（SRS）** | 导出 Anki CSV，让 Anki 负责 |
| **账号 / 云同步** | 本地 JSON + 可导出。数据不被应用锁死 |
| **PDF** | 固定版式，选中 / 重排 / 定位三件事都难，收益不成比例 |
| **标签** | 目前靠文档 + lemma 分组够用 |
| **多窗口** | 单窗口够用 |
| **完整词典下载** | 实测全量要 1.46GB 内存且词形还原反而失效，见 [`DICTIONARY.md`](DICTIONARY.md) |

---

## 已实测、不用再纠结的事

**不需要虚拟滚动。** 实测（`.dict-src/perf.ts`，真实 Electron 里跑）：

| 书 | 大小 | 打开 | 渲染完 | 段落数 | 滚动响应 | 选中响应 |
|---|---|---|---|---|---|---|
| 普通 | 0.9 MB | 228 ms | 239 ms | 4,420 | 1 ms | 17 ms |
| 中等 | 2.8 MB | 467 ms | 481 ms | 13,260 | 1 ms | 26 ms |
| 大 | 9.5 MB | 1,225 ms | 1,258 ms | 44,199 | 1 ms | 67 ms |

结论：现实中的书（3MB 以内）半秒内开完，全量渲染 React 节点完全撑得住。
等真撞上 10MB+ 的极端文件再说，现在加虚拟滚动是过度设计。

**释义收敛的效果**、**词库的体积与内存**、**Electron 包体代价** 等数据分别见
[`EXPORT.md`](EXPORT.md)、[`DICTIONARY.md`](DICTIONARY.md)、[`MVP.md`](MVP.md)。
